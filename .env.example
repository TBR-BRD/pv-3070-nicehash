# Reserved for future secrets or deployment-specific values.
# Do not put credentials into config.yaml committed to Git.

# Optional: NiceHash Platform (cloud) REST API, used to show hashrate/
# algorithm/profitability in the dashboard. Create a read-only key at
# nicehash.com -> Settings -> API Keys (Wallet: read, Mining: read).
NICEHASH_ORG_ID=
NICEHASH_API_KEY=
NICEHASH_API_SECRET=
